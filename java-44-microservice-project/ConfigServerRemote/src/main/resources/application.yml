server:
  port: 8889

spring:
  application:
    name: config-server-remote
  cloud:
    config:
      server:
        git:
          uri: ${REMOTE_CONFIG_PATH_PROJECT:https://github.com/aydindemir1/config-server-remote-microservice-project-2026.git}
          default-label: main
          username: ${REMOTE_USERNAME}
          password: ${REMOTE_TOKEN_PASSWORD}
